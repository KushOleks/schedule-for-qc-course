# Performance Report

## Цільовий додаток

- Назва: k6 Test Site
- URL: https://test.k6.io
- Інструмент: k6
- Дата: 2026-05-18

## Параметри тесту

- Кількість віртуальних користувачів: до 10 VUs
- Тривалість тесту: 50 секунд
- Сценарій:
  - 10 секунд — розігрів до 5 VUs
  - 30 секунд — навантаження до 10 VUs
  - 10 секунд — завершення до 0 VUs

## Перевірені ендпоінти

- GET /
- GET /contacts.php
- GET /news.php

## Thresholds

| Метрика | Поріг | Результат |
|---|---:|---:|
| http_req_duration p(95) | < 1000 ms | 156.86 ms |
| http_req_failed | < 5% | 0.00% |

## Результати

| Метрика | Значення |
|---|---:|
| checks_total | 955 |
| checks_succeeded | 955 / 955 |
| checks_failed | 0 |
| http_reqs | 1146 |
| http_req_duration avg | 89.5 ms |
| http_req_duration min | 26.7 ms |
| http_req_duration med | 118.39 ms |
| http_req_duration max | 496.28 ms |
| http_req_duration p(90) | 155.78 ms |
| http_req_duration p(95) | 156.86 ms |
| http_req_failed | 0.00% |
| iterations | 191 |
| vus_max | 10 |
| data_received | 1.5 MB |
| data_sent | 171 kB |

## Аналіз

Під час тестування було виконано навантажувальний тест для трьох сторінок тестового сайту k6. Навантаження поступово збільшувалось до 10 віртуальних користувачів, після чого зменшувалось до нуля.

Усі перевірки виконались успішно: головна сторінка, сторінка контактів та сторінка новин повернули статус 200. Частка помилкових HTTP-запитів становила 0.00%, тобто помилок під час тестування не було.

Середній час відповіді становив 89.5 ms, а 95-й перцентиль — 156.86 ms, що значно менше встановленого порогу 1000 ms.

## Висновок

Тестований додаток успішно витримав навантаження до 10 віртуальних користувачів. Усі thresholds були пройдені: час відповіді залишився в межах допустимого значення, а частка помилкових запитів дорівнювала 0%. Отже, продуктивність додатку в межах заданого навантаження є стабільною.