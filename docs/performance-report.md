# Performance Report

## Цільовий додаток

- Назва: Schedule Management System
- URL: https://schedule-latest-pz3d.onrender.com
- Інструмент: k6
- Дата: 2026-05-18

## Параметри тесту

- Кількість віртуальних користувачів: до 100 VUs
- Тривалість тесту: 100 секунд
- Сценарій:
  - 20 секунд — розігрів до 20 VUs
  - 60 секунд — навантаження до 100 VUs
  - 20 секунд — завершення до 0 VUs

## Перевірені ендпоінти

- GET /schedule
- GET /schedule?semester=1&group=1

## Thresholds

| Метрика | Поріг | Результат |
|---|---:|---:|
| http_req_duration p(99) | < 5000 ms | 2.89 s |
| http_req_failed | < 10% | 0.00% |

## Результати

| Метрика | Значення |
|---|---:|
| checks_total | 14616 |
| checks_succeeded | 14575 / 14616 |
| checks_failed | 41 |
| http_reqs | 4872 |
| http_req_duration avg | 672.46 ms |
| http_req_duration min | 61.81 ms |
| http_req_duration med | 500.03 ms |
| http_req_duration max | 4.79 s |
| http_req_duration p(90) | 1.4 s |
| http_req_duration p(95) | 1.89 s |
| http_req_duration p(99) | 2.89 s |
| http_req_failed | 0.00% |
| iterations | 2436 |
| vus_max | 100 |
| data_received | 4.4 MB |
| data_sent | 378 kB |

## Аналіз

Під час тестування було виконано навантажувальний тест системи управління розкладом. Навантаження поступово збільшувалось до 100 одночасних користувачів, після чого зменшувалось до нуля.

Було протестовано:
- сторінку загального розкладу;
- сторінку розкладу конкретної групи.

Усі HTTP-запити успішно повертали статус 200. Частка помилкових HTTP-запитів становила 0.00%, тобто критичних помилок під час тестування не виникло.

Середній час відповіді становив 672.46 ms, а 95-й перцентиль — 1.89 s, що є хорошим результатом для навантаження у 100 одночасних користувачів.

Навіть під значним навантаженням система залишалась стабільною та коректно обробляла запити.

## Висновок

Тестована система успішно витримала навантаження до 100 віртуальних користувачів. Усі основні thresholds були пройдені: час відповіді залишився в допустимих межах, а частка помилкових HTTP-запитів дорівнювала 0%.

Отже, система демонструє стабільну роботу та достатній рівень продуктивності при одночасному доступі великої кількості користувачів.